export const SHOP_SITE_URL = (process.env.NEXT_PUBLIC_SHOP_SITE_URL || 'https://shop.auronixcommerce.com').replace(/\/+$/, '');
export type ShopCategory = { id:string; name:string; slug:string; parentId?:string; description?:string; imageUrl?:string; published:boolean; sortOrder:number };
export type ShopProduct = { id:string; title:string; slug:string; shortDescription:string; description:string; categoryId:string; subcategoryId?:string; category?:string; subcategory?:string; brand:string; price?:number; oldPrice?:number; discountLabel?:string; currency:string; mainImageUrl:string; galleryImageUrls:string[]; amazonUrl:string; asin?:string; rating?:number; ratingCount?:string; bullets:string[]; specifications:Record<string,string>; availability?:string; tags:string[]; featured:boolean; badges:string[]; status:'draft'|'published'|'archived'; seoTitle?:string; seoDescription?:string; canonicalUrl?:string; createdAt:number; updatedAt:number };
export const emptyProduct = ():Omit<ShopProduct,'id'|'createdAt'|'updatedAt'> => ({title:'',slug:'',shortDescription:'',description:'',categoryId:'',brand:'',currency:'USD',mainImageUrl:'',galleryImageUrls:[],amazonUrl:'',bullets:[],specifications:{},tags:[],featured:false,badges:[],status:'draft'});
export function formatPrice(value?:number,currency='USD'){if(typeof value!=='number'||Number.isNaN(value))return null;return new Intl.NumberFormat('en-US',{style:'currency',currency,maximumFractionDigits:2}).format(value);}
export function safeAmazonUrl(value:string){try{const url=new URL(value);return url.protocol==='https:'?url.toString():null;}catch{return null;}}
