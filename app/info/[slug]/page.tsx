import type{Metadata}from'next';import{notFound}from'next/navigation';import{InfoPage}from'@/components/InfoPage';import{PUBLIC_PAGES,publicPage}from'@/lib/public-pages';
export function generateStaticParams(){return PUBLIC_PAGES.map(({slug})=>({slug}))}export function generateMetadata({params}:{params:{slug:string}}):Metadata{const page=publicPage(params.slug);return page?{title:page.title,description:page.intro}:{};}
export default function Page({params}:{params:{slug:string}}){const page=publicPage(params.slug);if(!page)notFound();return <InfoPage eyebrow={page.eyebrow} title={page.title} intro={page.intro}><div className="detailedSections">{page.sections.map((section,index)=><article className="detailSection" key={section.title}><span>0{index+1}</span><div><h2>{section.title}</h2><p>{section.body}</p></div></article>)}</div><aside className="officialCta"><div><strong>Need to contact Auronix Commerce?</strong><p>Use the separate official corporate website for company inquiries.</p></div><a className="cta" href="https://auronixcommerce.com/contact" target="_blank" rel="noopener noreferrer">Visit official website</a></aside></InfoPage>}
