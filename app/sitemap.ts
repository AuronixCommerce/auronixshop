import type{MetadataRoute}from'next';import{PUBLIC_PAGES}from'@/lib/public-pages';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://shop.auronixcommerce.com',core=['','/categories','/about','/how-it-works','/contact','/guides','/affiliate-disclosure','/privacy','/terms'],resources=PUBLIC_PAGES.map(page=>`/info/${page.slug}`);return[...core,...resources].map(path=>({url:`${base}${path}`,lastModified:new Date()}))}
