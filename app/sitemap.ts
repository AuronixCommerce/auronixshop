import type{MetadataRoute}from'next';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://shop.auronixcommerce.com';return['','/categories','/about','/how-it-works','/contact','/affiliate-disclosure','/privacy','/terms'].map(path=>({url:`${base}${path}`,lastModified:new Date()}))}
