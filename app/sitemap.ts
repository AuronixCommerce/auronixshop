import type{MetadataRoute}from'next';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://shop.auronixcommerce.com';return[{url:base,lastModified:new Date()},{url:`${base}/affiliate-disclosure`,lastModified:new Date()}]}
