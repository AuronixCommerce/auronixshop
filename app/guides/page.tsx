import Link from'next/link';import{InfoPage}from'@/components/InfoPage';import{PUBLIC_PAGES}from'@/lib/public-pages';export default function Page(){return <InfoPage eyebrow="Knowledge center" title="Shopping guides and policies" intro="Detailed information for understanding the storefront, researching products, and shopping through Amazon affiliate links."><div className="resourceGrid">{PUBLIC_PAGES.map(page=><Link className="resourceCard" href={`/info/${page.slug}`} key={page.slug}><span>{page.eyebrow}</span><h2>{page.title}</h2><p>{page.intro}</p><b>Read guide →</b></Link>)}</div></InfoPage>}
