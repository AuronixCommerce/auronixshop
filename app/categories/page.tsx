'use client';import Link from'next/link';import{Footer,Header}from'@/components/Chrome';import{useCatalog}from'@/lib/catalog';export default function Page(){const{categories,products,loading}=useCatalog();return <><Header/><main><section className="pageHero"><div className="wrap"><div className="eyebrow">Browse the collection</div><h1>Shop by category</h1><p>Explore useful departments, understand what each collection covers, and move directly into the matching products.</p></div></section><section className="section"><div className="wrap"><div className="contentGrid">{categories.map(c=><Link className="categoryTile" href={`/?category=${c.id}#catalog`} key={c.id}>{c.imageUrl&&<img src={c.imageUrl} alt=""/>}<h2>{c.name}</h2><p>{c.description||`Explore ${products.filter(p=>p.categoryId===c.id).length} currently available products in this category.`}</p><b>Browse category →</b></Link>)}{!loading&&categories.length===0&&<div className="empty"><h2>Collections are being prepared</h2><p>Please explore our buyer guides while new category collections become available.</p><Link href="/guides">Open shopping guides</Link></div>}</div><div className="officialCta"><div><strong>Looking for Auronix Commerce?</strong><p>Company, supplier, and partnership information is available on the separate official website.</p></div><a className="cta" href="https://auronixcommerce.com" target="_blank" rel="noopener noreferrer">Visit AuronixCommerce.com</a></div></div></section></main><Footer/></>}
