'use client';import Link from'next/link';import{Footer,Header}from'@/components/Chrome';import{useCatalog}from'@/lib/catalog';export default function Page(){const{categories,products,loading}=useCatalog();return <><Header/><main><section className="pageHero"><div className="wrap"><div className="eyebrow">Browse the collection</div><h1>Shop by category</h1><p>Explore the Auronix catalog through clear, useful product departments.</p></div></section><section className="section"><div className="wrap contentGrid">{categories.map(c=><Link className="categoryTile" href={`/?category=${c.id}#catalog`} key={c.id}>{c.imageUrl&&<img src={c.imageUrl} alt=""/>}<h2>{c.name}</h2><p>{c.description||`${products.filter(p=>p.categoryId===c.id).length} products in this category`}</p></Link>)}{!loading&&categories.length===0&&<div className="empty">Categories added in the admin workspace will appear here.</div>}</div></section></main><Footer/></>}
